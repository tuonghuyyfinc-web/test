Thư mục này để chứa hình ảnh nếu sau này bạn muốn thay thế các SVG inline bằng ảnh thật.

File Wanpachi 3D.html hiện tại hoàn toàn tự chứa (standalone) — mọi nhân vật, hiệu ứng điện, tia sét đều được vẽ bằng SVG và CSS inline, không cần file ngoài.

Nếu muốn chèn ảnh:
1. Đặt ảnh vào folder này
2. Trong HTML dùng: <img src="image/ten-anh.png">
